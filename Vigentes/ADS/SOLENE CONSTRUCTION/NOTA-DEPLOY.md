# Codigo a frente do que esta publicado

Esta pasta veio de `solene-site` (03/08/2026), que inclui `LeadForm.tsx` e
alteracoes em `App.tsx` e `Home.tsx` feitas depois do ultimo deploy.

Conferido em 29/08/2026 comparando os assets: o site em
www.soleneconstruction.com serve `index-DVLSscDA.js`, enquanto o build desta
versao gera `index-BFOVTNPE.js`. O CSS e o mesmo nos dois.

Ou seja: publicar esta pasta como esta coloca no ar o formulario de leads que
ainda nao foi ao ar. Confirme com o cliente antes do proximo deploy.
